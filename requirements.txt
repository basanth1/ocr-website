# recommended python version 3.10

# Core web framework
flask==3.0.3
flask-cors==4.0.1

# OCR / Computer vision
paddlepaddle>=2.7.0       # compatible with Python 3.10
paddleocr==2.7.3
opencv-python-headless==4.9.0.80
pdf2image==1.17.0
numpy==1.26.4
Pillow==10.3.0
shapely==2.0.3
lxml==5.2.2
beautifulsoup4==4.12.3
rapidfuzz==3.9.7

# Networking / APIs
httpx==0.27.2
python-dotenv==1.0.1

# Utilities
tqdm==4.66.4
setuptools
groq==0.11.0
# ocr_pipeline_studio/
# ├── run.py                  ← Start the server (python run.py)
# ├── requirements.txt
# ├── .env.example            ← Copy to .env, add your Groq key
# │
# ├── app/
# │   ├── main.py             ← Flask routes (/api/ocr, /api/sections, /api/download)
# │   ├── ocr_engine.py       ← PaddleOCR + PPStructure, all OCR logic
# │   └── section_builder.py  ← Regex/LLM headers, validation, section assembly
# │
# ├── templates/index.html    ← Full UI (served by Flask)
# ├── static/css/styles.css
# ├── static/js/app.js        ← All frontend logic, wired to API
# ├── uploads/                ← Temp storage (auto-cleaned after OCR)
# └── outputs/                ← All JSON/MD output files land here
